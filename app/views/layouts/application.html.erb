<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>Example Rails application with SimpleForm and Bootstrap</title>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!-- Favicon -->
    <%= favicon_link_tag 'favicon/favicon.png' %>

    <%= javascript_pack_tag 'hello_vue.js' %> 

  </head>
    <!--  Header  -->
    <%= render "header" %>
    <%= render "flash" %>

    
    <%= yield %>
    <% unless user_signed_in? %>
    <%= render "welcome" %>
    <% end %>
</html>
